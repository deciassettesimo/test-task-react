import { colors } from 'components/constants';

export default {
  item: {
    padding: '4px 8px',
    borderRadius: '2px',
    backgroundColors: {
      checked: colors.primary98,
      normal: colors.background,
    },
    borderColors: {
      checked: colors.primary90,
      normal: colors.neutral90,
    },
  },
};
