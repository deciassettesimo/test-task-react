import globals from 'application/globals';

export const captions = {
  title: globals.captions.pages.beers.main,
  errors: globals.captions.errors,
};

export const breadCrumbs = [globals.pages.home];
